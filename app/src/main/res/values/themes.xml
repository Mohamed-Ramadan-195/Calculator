<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Calculator" parent="android:Theme.Material.Light.NoActionBar" />

    // parent style
    <style name="calculateButton">
        <item name="android:textSize">@dimen/textButton</item>
        <item name="android:layout_width">@dimen/ButtonLayoutWidth</item>
        <item name="android:layout_height">@dimen/ButtonLayoutHeight</item>
    </style>

    // first child style
    <style name="calculateButton.Operation">
        <item name="android:background">@drawable/operation_button</item>
        <item name="android:textColor">@color/white</item>
    </style>

    // second child style
    <style name="calculateButton.Number">
        <item name="android:background">@drawable/number_button</item>
        <item name="android:textColor">@color/black</item>
    </style>

    // third child style
    <style name="calculateButton.Equal">
        <item name="android:background">@drawable/equal_button</item>
        <item name="android:textColor">@color/black</item>
    </style>

    // parent style
    <style name="advancedButtons">
        <item name="android:textSize">@dimen/textAdvancedButton</item>
        <item name="android:textColor">@color/white</item>
    </style>

    // first child style
    <style name="advancedButtons.Answer">
        <item name="android:background">@drawable/answer_button</item>
    </style>

    // second child style
    <style name="advancedButtons.advancedOperations">
        <item name="android:background">@drawable/advanced_operation_button</item>
    </style>

    <style name="viewStyle">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:layout_weight">1</item>
    </style>

</resources>